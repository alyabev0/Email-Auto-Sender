import React from 'react'
import { Spinner } from '@chakra-ui/react'
const CustomSpinner = () => {
    return (
        <React.Fragment>
            <Spinner></Spinner>
        </React.Fragment>
    )
}

export default CustomSpinner